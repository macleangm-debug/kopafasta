<style>
    /* Official docs: full-bleed header within page; body keeps Plus-like safe margins */
    @page { margin: 16mm 0 16mm 0; }
    body {
        font-family: DejaVu Sans, sans-serif;
        font-size: 10.5px;
        color: #111827;
        line-height: 1.5;
        margin: 0;
    }
    h1, h2, h3 { font-weight: 700; page-break-after: avoid; }
    h2 {
        font-size: 12px;
        margin: 14px 0 6px;
        color: #0f3d2e;
        border-bottom: 1px solid #e5ebe7;
        padding-bottom: 3px;
        page-break-after: avoid;
    }
    h3 {
        font-size: 11px;
        margin: 10px 0 4px;
        color: #0f3d2e;
        page-break-after: avoid;
    }
    p, li { orphans: 3; widows: 3; }
    .muted { color: #6b7c74; font-size: 9px; }
    .sw { color: #3d4f46; font-size: 9.5px; margin-top: 3px; }

    table.band {
        width: 100%;
        border-collapse: collapse;
        background-color: #0f3d2e;
        color: #ffffff;
        margin: 0;
    }
    .masthead { margin: -16mm 0 0 0; line-height: 1.05; }
    table.band td { line-height: 1.05; }
    table.band td.brand-cell { padding: 3px 14mm 2px; background-color: #0f3d2e; }
    table.band td.meta { padding: 3px 14mm 2px 6px; text-align: right; color: #e8eee9; font-size: 9px; line-height: 1.15; background-color: #0f3d2e; }
    .wordmark { font-size: 15px; color: #f5c842; font-weight: 700; line-height: 1; }
    .legal { font-size: 9px; color: #ffffff; margin-top: 1px; line-height: 1.1; }
    .tag { font-size: 9px; color: #d5ddd8; margin-top: 1px; font-weight: 700; line-height: 1.15; }
    .gold-bar { height: 3px; background-color: #f5c842; width: 100%; margin: 0; }
    .wrap { padding: 10px 16mm 18px; }
    .logo { max-height: 22px; max-width: 28px; }

    .notice { margin: 10px 0 12px; padding: 10px 12px; background: #f7faf8; border-left: 3px solid #f5c842; page-break-inside: avoid; }
    .notice strong { color: #0f3d2e; }

    table.kv { width: 100%; border-collapse: collapse; margin-bottom: 6px; }
    table.kv td { padding: 4px 5px; vertical-align: top; border-bottom: 1px solid #eef2ef; }
    table.kv td.label { color: #4d5f56; width: 38%; font-size: 9px; }
    table.kv td.value { color: #12241c; font-weight: 600; }

    table.grid { width: 100%; border-collapse: collapse; margin-top: 6px; font-size: 8.5px; }
    table.grid th, table.grid td { border: 1px solid #e5ebe7; padding: 4px 5px; }
    table.grid th { background: #f7faf8; font-size: 8px; color: #0f3d2e; text-align: left; }
    table.grid thead { display: table-header-group; }
    table.grid tr { page-break-inside: avoid; }

    .charges { width: 100%; border-collapse: collapse; font-size: 9.5px; margin-top: 6px; }
    .charges td { padding: 4px 6px; border: 1px solid #e5ebe7; vertical-align: top; }
    .charges td:first-child { background: #f7faf8; width: 42%; font-weight: 600; }
    .charges-total td { font-weight: 700; background: #eef6f1 !important; }

    .signbox {
        margin-top: 12px;
        padding: 8px 0;
        border: none;
        background: transparent;
        page-break-inside: avoid;
    }
    .sig-img {
        max-height: 48px;
        max-width: 150px;
        background: transparent !important;
        border: 0 !important;
    }
    .stamp-img {
        max-height: 72px;
        max-width: 72px;
        margin-top: 4px;
        background: transparent !important;
        border: 0 !important;
    }
    .na { color: #8a9a92; font-style: italic; }
    .annex { page-break-before: always; }
    ol.acks li { margin-bottom: 3px; }
    .pill { display: inline-block; padding: 1px 8px; background: #f5c842; color: #0f3d2e; font-size: 8.5px; font-weight: 700; line-height: 1.2; }

    .keep-together { page-break-inside: avoid; }
    .keep-with-next { page-break-after: avoid; }

    .doc-footer {
        margin-top: 10px;
        font-size: 8.5px;
        color: #6b7280;
    }
    .footer { margin-top: 10px; font-size: 8.5px; color: #6b7280; }
</style>
