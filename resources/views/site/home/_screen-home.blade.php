<div class="h-full text-white p-4 flex flex-col bg-gradient-to-b from-brand to-emerald-900">
    <p class="text-[9px] uppercase tracking-[0.18em] text-brand-gold font-bold">Kopafasta</p>
    <p class="text-lg font-black mt-2 leading-tight">{{ __('site.hero.showcase_member') }}</p>
    <p class="text-[10px] font-mono text-white/60 mt-0.5">{{ __('site.hero.showcase_member_no') }}</p>
    <div class="mt-4 rounded-2xl bg-white/10 ring-1 ring-white/15 p-3">
        <p class="text-[9px] uppercase tracking-widest text-white/60">Bronze · Plus</p>
        <p class="text-xl font-black tabular-nums text-brand-gold mt-1">TZS 2.4M</p>
        <p class="text-[10px] text-white/70">{{ __('borrower.dashboard.eligibility_growth_hint_short') }}</p>
    </div>
    <div class="mt-3 grid grid-cols-2 gap-2 text-[10px] font-semibold">
        <div class="rounded-xl bg-white/10 px-3 py-2">{{ __('borrower.nav.loans') }}</div>
        <div class="rounded-xl bg-white/10 px-3 py-2">{{ __('borrower.nav.marketplace_short') }}</div>
        <div class="rounded-xl bg-brand-gold/90 text-brand px-3 py-2">Plus</div>
        <div class="rounded-xl bg-white/10 px-3 py-2">{{ __('borrower.nav.rewards') }}</div>
    </div>
    <div class="mt-auto rounded-xl bg-white/10 p-3 text-[11px] text-white/80">{{ __('site.hero.title') }}</div>
</div>
